/**
 * Follow-up Draft Agent (placeholder — will be built next)
 */
export async function runFollowUpDraft(ai: Ai, input: { thread: string }) {
  return { success: false, message: 'Follow-up draft workflow coming soon.' };
}
