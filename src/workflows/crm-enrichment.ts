/**
 * CRM Enrichment Agent (placeholder — will be built next)
 */
export async function runCrmEnrichment(ai: Ai, input: { company: string; url?: string }) {
  return { success: false, message: 'CRM enrichment workflow coming soon.' };
}
